name: AI Guardian
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  balance-guardian:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm run ai:guard
