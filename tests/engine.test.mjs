import test from'node:test';import assert from'node:assert/strict';import{FishingEngine}from'../src/core/fishing-engine.js';
test('comentários simultâneos acumulam pontos por usuário',()=>{const e=new FishingEngine({random:()=>.2,now:(()=>{let n=0;return()=>++n})()});for(let i=0;i<20;i++)e.catch({username:'ana',source:'comment'});for(let i=0;i<10;i++)e.catch({username:'bia',source:'comment'});assert.equal(e.totalCatches,30);assert.equal(e.players.size,2);assert.ok(e.player('ana').points>e.player('bia').points)});test('presente aumenta luck mas respeita teto',()=>{const e=new FishingEngine({random:()=>.5});const r=e.catch({username:'donor',source:'gift',diamonds:100000,count:99});assert.ok(r.luck<=1.35)});